LANG = "en"
output = "wrong language" if LANG == "en" else "good language"
print(output)
#one more example of an if statement, more condensed than the first
#all caps variable is a convention for constants